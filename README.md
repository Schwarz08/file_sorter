# file_sorter
 Sorts files into specified folders. To identify a file, the input can be the complete file name or a part of the file name. A file name or file name keyword must be unique, if multiple instances of a file name or file name keyword are found, the tool will not sort them.
